{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build server (debug)",
      "type": "shell",
      "command": "make PROFILE=debug server",
      "options": {
        "cwd": "${workspaceFolder}/src",
      },
      "problemMatcher": ["$gcc"]
    },
    {
      "label": "build client (debug)",
      "type": "shell",
      "command": "make PROFILE=debug client",
      "options": {
        "cwd": "${workspaceFolder}/src",
      },
      "problemMatcher": ["$gcc"]
    },
  ],
}
