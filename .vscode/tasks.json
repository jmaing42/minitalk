{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build server (debug)",
      "type": "shell",
      "command": "make PROFILE=debug server",
      "options": {
        "cwd": "${workspaceFolder}/src",
      },
      "problemMatcher": ["$gcc"]
    },
  ],
}
